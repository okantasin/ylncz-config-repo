spring:

  data:
    mongodb:
      uri: mongodb://localhost:27017/users-service
  main:
    allow-bean-definition-overriding: true
    web-application-type: reactive

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
